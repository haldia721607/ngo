var smm = (function (scope) {
    var listGrid;
    scope.listItems = [];
    scope.url = `/admin`;
    scope.eventId = 0;
    scope.Event = [];
    window.location.search.substring(1).split("&").forEach(function (qs) {
        var arr = qs.split("=");
    })

    return scope;
})(smm || {});

